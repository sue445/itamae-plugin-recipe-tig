require 'spec_helper'

describe Itamae::Plugin::Recipe::Tig do
  it 'has a version number' do
    expect(Itamae::Plugin::Recipe::Tig::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
